-#coding: utf-8
- provide(:title, "Record de la Factura")
%h1 Factura:
.row
  %aside.span6
    %section
      %h5
        -if @debtor
          %ol.microposts
            = render @debt #render partial: 'debts', :locals => { :collection => @collection }   
          %em Deudor:            
          = link_to @debtor.name, @debtor
          ="   |  Persona Contacto: "
          = @debtor.contact_person
        /end  

  %aside.span4
    %section
      =#yield :logs_sidebar
    %p
      %a{:href => '#{url_for new_collection_log_path(@debt)}', :class => "btn btn-large btn-primary disabled"}
        Añadir Plan de Pago
      %span{:class => 'glyphicon glyphicon-envelope'}
        = button_to "Generar Primer Aviso", home_path, :class => "btn btn-large btn-primary disabled", :method => :get, params: {email: current_user.email}