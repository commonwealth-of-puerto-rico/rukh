-#coding: utf-8
- provide(:title, "Record de la Factura")
%h1 Factura:
.row
  %aside.span6
    %section
      %h5
        -if @debtor
          %ol.microposts
            = render @debt #render partial: 'debts', :locals => { :collection => @collection }   
          %em Deudor:            
          = link_to @debtor.name, @debtor
          ="   |  Persona Contacto: "
          = @debtor.contact_person
        /end  

  %aside.span4
    %section
      =#yield :logs_sidebar
    %p
      %a{:href => '#{url_for new_payment_plan_path(@debt)}', :class => "btn btn-large btn-primary disabled"}
        Añadir Plan de Pago
      %span{:class => 'glyphicon glyphicon-envelope'}
        = button_to "Generar Primer Aviso", email_send_path, :class => "btn btn-large btn-primary", :method => :get, params: {email: current_user.email, mailer: 'first_notification'}
    %p
      = link_to "Vista preliminar de Emails", rails_email_preview.rep_emails_url(email_locale: 'es', debt_id: @debt.id, current_user: @user)